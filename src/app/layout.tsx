import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "NV Web Design — Affordable Websites for NZ Trades Businesses",
  description:
    "Professional websites and hosting for New Zealand plumbers, electricians, landscapers and trades businesses. Get online from $1,200.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
